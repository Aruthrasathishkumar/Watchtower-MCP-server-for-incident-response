"""WatchTower MCP tool implementations.

Each module in this package implements one MCP tool:
  - the SQL + parameters
  - the JSON schema for Claude
  - the handler function called by the MCP server
"""